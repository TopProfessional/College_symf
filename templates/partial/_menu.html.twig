<div>
    <ul class="header_menu">
        {% for item in items %}
            <li class="{{item.class}}">
                <a href="{{path(item.route_name)}}">{{item.label}}</a>
            </li>
        {% endfor %}
    </ul>
</div>
