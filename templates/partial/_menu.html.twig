<div>
    <ul class="header-menu">
        {% for item in items %}
            <li class="{{item.style_class_name}}">
                <a href="{{path(item.route_name)}}">{{item.label}}</a>
            </li>
        {% endfor %}
    </ul>
</div>
