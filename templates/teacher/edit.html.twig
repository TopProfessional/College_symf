{% extends 'base.html.twig' %}
{% set isNew = not ( form.vars.data and form.vars.data.id ) %}

{% block title %}
    {% if isNew %}
        Add
    {% else %}
        Edit*
    {% endif %}
        Teacher
{% endblock %}

{% block body %}

    <h1>{{ block('title') }}</h1>

    {{ include('partial/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('teacher_index') }}">back to list</a>

    {% if not isNew %}
        {{ include('partial/_delete_form.html.twig', {delete_route: 'teacher_delete', entity: teacher}) }}
    {% endif %}
{% endblock %}
